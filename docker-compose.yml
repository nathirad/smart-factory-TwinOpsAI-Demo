services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    env_file:
      - .env
    environment:
      APP_MODE: ${APP_MODE:-mock}
      BACKEND_API_ENDPOINT: http://backend:8000/api/ingest-telemetry
    ports:
      - "8000:8000"
    restart: unless-stopped
